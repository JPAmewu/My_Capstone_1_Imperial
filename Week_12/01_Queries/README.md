# Week 12 queries

This folder contains the eight reconciled queries submitted in Week 12:

- [`week_12_query_points.txt`](week_12_query_points.txt)

The coordinates were recovered from the supplied cumulative `week_12_inputs`
snapshot. All 88 historical prefix pairs matched the immutable ledger, and the
eight aligned Week 12 scalar returns were recovered from the companion
`week_12_outputs` snapshot. The confirmed pairs are now recorded in
[`../../Results/query_output_ledger.csv`](../../Results/query_output_ledger.csv).

The previously tracked file repeated the Week 11 queries and matched none of
the supplied Week 12 inputs. It is retained under `archive/` as historical
evidence of the reconciliation rather than silently overwritten.
