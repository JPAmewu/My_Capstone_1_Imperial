# Week 01 Function 08 results

- `observations.csv` contains the 40 starter rows with inputs, output, query
  number, and running best.
- `summary.json` records the validated count, dimensionality, starter incumbent,
  latest starter output, input bounds, and Bayesian Optimisation strategy label.

Both files mirror the Week 1 main-notebook baseline and are generated by
`../02_Code/analyse_function_08.py`. The improving ledger return is reserved for
later cumulative checkpoints.
