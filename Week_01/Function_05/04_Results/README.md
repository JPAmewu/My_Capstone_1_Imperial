# Week 01 Function 05 results

- `observations.csv` contains the 20 starter rows with inputs, output, query
  number, and running best.
- `summary.json` records the validated count, dimensionality, incumbent, latest
  starter output, input bounds, and Manual Reasoning strategy.

Both files mirror the Week 1 main-notebook baseline and are generated by
`../02_Code/analyse_function_05.py`.
