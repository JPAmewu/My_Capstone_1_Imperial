# Week 01 Function 07 results

- `observations.csv` contains the 30 starter rows with inputs, output, query
  number, and running best.
- `summary.json` records the validated count, dimensionality, incumbent, latest
  starter output, input bounds, and Bayesian Optimisation strategy label.

Both files mirror the Week 1 main-notebook baseline and are generated by
`../02_Code/analyse_function_07.py`.
